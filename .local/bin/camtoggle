#!/bin/sh

camera=$(find /dev/video[02468] | tail -n 1)

if [ -e "$camera" ]; then
  pkill -f "$camera" || mpv --quiet --profile=low-latency --untimed "$camera"
else
  notify-send "No camera detected"
fi
