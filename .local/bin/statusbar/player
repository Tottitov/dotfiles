#!/bin/sh

song=$(mpc current)
[ -z "$song" ] && exit 1

song=$(echo "$song" | sed 's/^[^-]* - //')
state=$(mpc status | rg -o 'paused|playing' | sed 's/^./\U&/')

[ "$state" = "Paused" ] && color="#f7768e" || color="#9ece6a"

echo "<span color=\"$color\">󰎇 $state: $song</span>"
