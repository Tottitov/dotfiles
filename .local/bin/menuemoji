#!/bin/sh

emojis=$(cat <"${XDG_DATA_HOME}/unicode")
emoji=$(echo "$emojis" | fuzzel --only-match --cache "$XDG_CACHE_HOME/unicode" -w 20 -di) || exit 0

echo "$emoji" | cut -d ' ' -f1 | tr -d '\n' | wl-copy && notify-send "Emoji copied to clipboard" "$emoji"
