#!/bin/sh

emoji=$(
	cat <"${XDG_DATA_HOME}/unicode" | sort | fuzzel \
		--cache "$XDG_CACHE_HOME/unicode" \
		-w 20 -dip "Emoji: "
) || exit 1

echo "$emoji" | cut -d ' ' -f 1 | tr -d '\n' | wl-copy
notify-send "Emoji copied to clipboard" "$emoji"
